#lang racket
(provide (all-defined-out))

(define x 3) ;val x = 3
(define y (+ 2 x)) ; val y = (2 + x) ; + is a function call

(define cube1
  (lambda (x)
    (* x (* x x))))

(define cube2
  (lambda (x)
    (* x x x)))

(define (cube3 x)
  (* x x x))
